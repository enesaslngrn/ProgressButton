<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProgressIndicators" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ProgressIndicators" parent="Base.Theme.ProgressIndicators" />

    <style name="circularProgressBar" parent="Widget.MaterialComponents.CircularProgressIndicator">
        <item name="indicatorColor">@color/white</item>
        <item name="indicatorSize">24dp</item>
        <item name="trackThickness">3dp</item>
        <item name="android:indeterminate">true</item>
    </style>

    <style name="progressButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/button_disable_background</item>
        <item name="shapeAppearanceOverlay">@style/progressButtonShape</item>
    </style>

    <style name="progressButtonShape" parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cornerSize">30%</item>
    </style>
</resources>